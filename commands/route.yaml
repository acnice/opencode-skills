name: route
description: Route any query to the optimal agent via omni_route
arguments:
  - name: query
    description: The user query to route
    required: true
    type: string

steps:
  - tool: skill
    name: omni_route
    arguments:
      query: "{{query}}"

output:
  format: markdown